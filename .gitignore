# Next.js
.next/
.next-beta-test/
apps/*/.next-beta-test/
.next-turbo/
.next-sendero/
apps/*/.next-turbo/
apps/*/.next-sendero/
apps/*/.next.root-owned.*/
apps/*/.next-sendero.root-owned.*/
apps/*/.next-user/
apps/*/.next-e2e/
apps/*/tmp/
apps/*/.next-turbo/
apps/*/.next-beta-test/
.cache-sendero/
apps/*/.cache-sendero/
apps/*/.cache-user/
apps/*/.cache-storybook/
apps/*/.ponder-sendero/
apps/*/.ponder-local/
apps/*/.ponder-user/
out/
*.tsbuildinfo

# Dependencies
node_modules/

# Local env (credentials)
.env
.env.local
.env.*.local

# Bootstrap state
.bootstrap-seed-progress.json

# Stray root-level forge install (canonical libs live in contracts/lib/)
/lib/

# Fumadocs generated source manifest
apps/docs/.source/

# Logs
*.log
.gstack/
.vercel

# Build output
apps/storybook/storybook-static/

# Turbo cache
.turbo/

# Local agent scratch (per-session, not repo state)
.agents/
.claude/

# Env backups (created by /vercel:bootstrap before destructive env changes)
.env.local.backup-*

# Turborepo
.turbo
.env*.local

# Playwright
apps/app/test-results/
apps/app/playwright-report/

# Storybook build output
apps/storybook/storybook-static/

# Local dev credentials (never commit — contains passwords)
qa-logins.local.json
.env.local.bak*
*.local.json

# Temporary files from vercel/ops scripts
.tmp-*.json
.wrangler/

# Generated OG/share image previews (regenerable artifacts)
og-previews/

# macOS Finder noise
.DS_Store
**/.DS_Store

# Self-hosted Phoenix data dir (packages/arize-phoenix/docker-compose.yml)
.phoenix-data/
packages/arize-phoenix/.phoenix-data/

# Solana e2e test-mint secret key (devnet — regenerable, but still a private key)
apps/admin/scripts/_local/.solana-test-usdc-mint.json
