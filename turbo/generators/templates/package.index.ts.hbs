/**
 * @sendero/{{name}} — {{description}}
 *
 * Remember to register the path alias in:
 *   - root tsconfig.json
 *   - every apps/<app>/tsconfig.json that consumes it
 * and to list `"@sendero/{{name}}": "workspace:*"` under the
 * consuming apps' `dependencies`.
 */

export const PACKAGE_NAME = '@sendero/{{name}}';
