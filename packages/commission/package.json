{
  "name": "@sendero/commission",
  "version": "0.1.0",
  "private": true,
  "description": "Sendero — per-tenant commission schedules + atomic split settlement on Arc. Computes supplier / agency / rail / validator legs for a booking gross, writes legs to Prisma, fires a single on-chain fan-out via @sendero/tools/settle-split.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schedule": "./src/schedule.ts",
    "./split": "./src/split.ts"
  },
  "peerDependencies": {
    "@sendero/database": "*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
