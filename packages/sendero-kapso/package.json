{
  "name": "@sendero/kapso",
  "version": "0.1.0",
  "private": true,
  "description": "Sendero — Kapso platform client for BYO WhatsApp onboarding. Thin typed wrapper around the Kapso HTTP API: customers, setup links, whatsapp connections, phone-number webhooks, and signature verification. No business logic, no I/O beyond fetch. Ported from desk-v1, adapted for Sendero.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts",
    "./setup-link": "./src/setup-link.ts",
    "./webhook": "./src/webhook.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
