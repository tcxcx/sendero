.anchor/
target/
node_modules/
test-ledger/
.DS_Store
*.log

# Cargo lockfile — gitignored during hackathon velocity. `anchor build`
# regenerates from Cargo.toml; commit as a separate chore when
# reproducibility / supply-chain pinning becomes needed.
Cargo.lock

# Generated IDLs land in target/idl — re-generated by `anchor build`.
# Keep them out of git; consumers regenerate locally or pin a release tag.
