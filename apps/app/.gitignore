.env*.local

# QA / browse-skill screenshot output (generated by /qa runs and ad-hoc
# browse smoke tests). Lives under apps/app so the screenshot tool can
# write under the dev server's CWD. Never checked in.
.gstack-screenshots/
