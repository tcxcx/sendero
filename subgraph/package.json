{
  "name": "sendero-subgraph",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goldsky": "goldsky subgraph deploy sendero-guest-escrow/v0.1.0",
    "deploy:hosted": "graph deploy --product hosted-service",
    "deploy:studio": "graph deploy --studio sendero-guest-escrow",
    "sync-abi": "node ./scripts/sync-abi.js",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.0",
    "@graphprotocol/graph-ts": "^0.39.0"
  }
}
